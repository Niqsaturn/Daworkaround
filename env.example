# The payout address is hardcoded as a safe execution fallback in source.
# Set BTC_ADDRESS only if you intentionally want to override it.
BTC_ADDRESS=bc1qtlqwdh6va8x50ax5nmgt4hq8ca66qsrv9l4k0c
WORKER_NAME=chatgpt
POOL_HOST=stratum.ckpool.org
POOL_PORT=3333
POOL_PASSWORD=x
HTTP_PORT=8787
BIND_HOST=127.0.0.1
AUTO_CONNECT=true
SESSION_TTL_MS=21600000
ALLOW_ORIGIN=*
# REQUIRED before exposing the HTTP bridge publicly.
BRIDGE_TOKEN=replace-with-a-long-random-token
